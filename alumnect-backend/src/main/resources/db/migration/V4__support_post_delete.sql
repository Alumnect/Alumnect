-- Migration for UC23: Delete a Post
-- The 'DELETED' status is already supported in the 'ck_posts_status' constraint 
-- from V1__init_database_schema.sql. No structural changes are required.

SELECT 1;
