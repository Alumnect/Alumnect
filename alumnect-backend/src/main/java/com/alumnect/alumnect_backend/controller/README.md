# Package: `com.alumnect.alumnect_backend.controller`

## 📋 Overview
REST API Presentation Layer. Receives HTTP requests, validates DTOs (@Valid), calls Service logic, and wraps responses in ResponseEntity.
