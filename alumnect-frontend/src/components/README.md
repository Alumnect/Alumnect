# Directory: `src/components`

## 📋 Overview
Shared stateless UI components (Button, Input, Modal, DataTable, Toast) built with Tailwind CSS. These components contain no API logic and rely strictly on props.
